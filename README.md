# customer-intake-powerautomate
Automates customer requests from intake (Forms/SharePoint) through approvals and creates/updates records in CRM (Dataverse or HTTP API), with auditing, SLAs, and retry handling.
